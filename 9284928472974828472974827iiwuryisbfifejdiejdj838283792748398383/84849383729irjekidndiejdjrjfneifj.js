bootmation = [boottxt = document.getElementById("boottxt"),boottxt.innerText = "_",setTimeout(() => {  boottxt.innerText = "grub_"}, 100), setTimeout(() => {  boottxt.innerText = "grub o"}, 350), setTimeout(() => {  boottxt.innerText = "grub open"}, 500), setTimeout(() => {  boottxt.innerText = "grub open_"  }, 850), setTimeout(() => {  boottxt.innerText = "booting_"}, 1200), setTimeout(() => {  boottxt.innerText = "booting"}, 2000), setTimeout(() => {  boottxt.innerText = "booting_"}, 2500), ]; setTimeout(() => {userTime() }, 1000);  function userTime() { const timee = document.getElementById("time"); const d = new Date(); let timenum = d.toLocaleTimeString(); timee.innerText = timenum;setTimeout(() => { userTime() }, 1000); }  function closeapp(Closeapp) {document.getElementById(Closeapp.id).classList.add("hide");};function view(viewapp) { document.getElementById(viewapp.id).classList.toggle("fullscreen"); viewapp.style.top = "0.5em";viewapp.style.left = "0.5em";} function openapp(Openapp){  document.getElementById(Openapp.id).classList.remove("hide");document.getElementById("menu").classList.remove("open"); Openapp.style.top = "20vh";Openapp.style.right = "35vw"; }function ico(icon1, icon2) {  document.getElementById(icon1.id).classList.toggle("hide"); document.getElementById(icon2.id).classList.toggle("hide")}; screen = document.getElementById("screen");  function dragWin(elemt) {var box = document.getElementById(elemt.id); box.addEventListener('touchmove', function(e) {  box.classList.add('layer'); var touchLocation = e.targetTouches[0];  box.style.left = touchLocation.pageX - 250 + 'px';  box.style.top = touchLocation.pageY - 250 + 'px';}); box.addEventListener('touchend',function(e) { box.classList.remove('layer'); var x = parseInt(box.style.left); var y = parseInt(box.style.top);  box.removeEventListener('touchend');box.removeEventListener('touchmove');})}; 
